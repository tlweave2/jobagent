# Example environment variables for the job agent

# Primary and backup AI models
# Options for AI_PRIMARY might include 'local' or 'cloud'
# Options for AI_BACKUP might include 'claude', 'gpt-4' or 'none'
AI_PRIMARY=local
AI_BACKUP=claude

# API keys for cloud models (if required)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Other configuration values can be added here